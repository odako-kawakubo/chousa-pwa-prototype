# 調査システムPWA プロトタイプ v0.8.5

## 更新方式

v0.8.5から入口ページ方式です。

- `index.html`：固定の入口ページ
- `version.json`：最新版の本体ファイルを指定
- `app-v0.8.5.html`：アプリ本体

iPadやホーム画面では、今まで通りリポジトリ直下のURLを開けばOKです。
毎回 `?v=...` を入力する必要はありません。

アプリ内の「更新確認」も、キャッシュ削除ではなく最新版URLへ移動する方式に変更しています。
