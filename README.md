# 調査システムPWA プロトタイプ v0.9.0


## ファイル構成

- `index.html`：固定の入口ページ
- `app.html`：常に最新版の本体
- `version.json`：最新版情報
- `README.md`：この説明

## 更新運用

通常更新は `app.html` と `version.json` を差し替えます。
`index.html` は基本的に固定です。

アプリが古いままの場合は、入口またはアプリ内の「最新版に更新」を押します。
アプリ本体キャッシュだけを削除し、案件データや未同期データは削除しません。
